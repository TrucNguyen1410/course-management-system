@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Nếu đang không ở trang login thì đá về trang login
        if (!Navigation.Uri.ToLower().Contains("login"))
        {
            Navigation.NavigateTo("login", true);
        }
    }
}